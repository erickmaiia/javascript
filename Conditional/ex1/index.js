/* Create a code that receives any numeric const. Create an if to check if the number stored in the const is even. If so, print the message on the console: "the number is even". */

const x = prompt("Write a number: ");
if (x % 2 === 0) {
  alert("This number is pair");
} else {
  alert("This number is not pair");
}
