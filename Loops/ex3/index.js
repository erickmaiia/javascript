/* Create an array with 5 strings. Write a program that traverses this array and prints the strings in ranking format, as in the example below: */

const bigCountries = ["Russia", "Canada", "China", "EUA", "Brazil"];

for (i = 0; i < bigCountries.length; i++) {
  console.log(bigCountries[i]);
}
